# python program to sort the dictionary
from typing import Container


print("pythom porgram to d=sort the  dictionary")
b=""
s={1:20,8:30,5:23,9:874}
Container=0
for ele in sorted(s.keys()):
    print(ele)
    a=str(ele)
    b=b+a
print(f"The keys of the given dict in the acending order:{b}")